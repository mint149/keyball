name: Build a firmware on demand(v0.28)

on:
  workflow_dispatch:
    inputs:
      keyboard:
        description: 'Keyboard'
        required: true
        type: choice
        options:
        - keyball39
        - keyball44
        - keyball61
        - keyball46
        - one47
        default: 'keyball44'
      keymap:
        description: 'Keymap'
        required: true
        type: choice
        options:
        - default
        - develop
        - pigeon
        default: 'pigeon'

jobs:

  build:

    uses: ./.github/workflows/build-firmware.yml
    with:
      qmk_version: '0.28.10'
      keyboard:    ${{ inputs.keyboard }}
      keymap:      ${{ inputs.keymap }}
